<%- include('head') %><h1>DB復元の確認</h1><p><%= new Date(backup.createdAt).toLocaleString('ja-JP',{timeZone:'Asia/Tokyo'}) %> の状態へ戻します。</p><p>現在の状態も自動バックアップされます。現在のDB全体が置き換わります。</p><p><%= backup.name %></p><p>products: <%= backup.products %> / listings: <%= backup.listings %></p><form method="post" action="/backups/restore"><input type="hidden" name="_csrf" value="<%= csrf %>"><input type="hidden" name="filename" value="<%= backup.name %>"><input type="hidden" name="confirm" value="yes"><button>この状態へ復元する</button> <a href="/backups">キャンセル</a></form><%- include('foot') %>