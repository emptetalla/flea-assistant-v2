const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const vm=require('node:vm');
test('scroll restored after confirm, empty query normalization, unmatched filters ignored',()=>{let saved=null,submit;const table={scrollTop:540,scrollLeft:80};const win={scrollY:300,scrollTo(x,y){this.scrollY=y;}};const context={document:{getElementById:()=>({addEventListener:(n,f)=>{submit=f;}}),querySelector:()=>table},location:{search:'?category=unset'},URLSearchParams,Date,requestAnimationFrame:f=>f(),sessionStorage:{setItem:(k,v)=>{saved=v;},getItem:()=>saved,removeItem:()=>{saved=null;}},window:win};const run=()=>vm.runInNewContext(fs.readFileSync('public/list-position.js','utf8'),context);run();submit({defaultPrevented:false});table.scrollTop=0;table.scrollLeft=0;win.scrollY=0;context.location.search='?q=&category=unset&updated=2';run();assert.equal(table.scrollTop,540);assert.equal(table.scrollLeft,80);assert.equal(win.scrollY,300);assert.equal(saved,null);submit({defaultPrevented:false});context.location.search='?category=1';table.scrollTop=0;run();assert.equal(table.scrollTop,0);});
